---
title: Packages Overview
description: A map of the package docs, the current import model, and where to go next.
---

Mog uses `@import(...)` for both source modules and native packages. The package
manager supports local, workspace, Git, and registry dependencies with lockfiles,
offline installs, signed registry metadata, publishing, and advisory audits.

## Read by task

- [Using Native Packages](/docs/packages/using-native-packages/) for package
  imports, search paths, opaque handles, and validation.
- [Authoring Native Packages](/docs/packages/authoring-native-packages/) for
  manifests, `package.api.mog`, and reference package layout.
- [Window Package](/docs/packages/window-package/) for the first official
  runtime-maintained package.

## Import model at a glance

- string literal paths such as `"./math.mog"` resolve as source modules
- hosted module paths such as `"github.com/acme/math"` resolve through
  installed Git module metadata
- bare names such as `"math"` and `"window"` resolve as packages
- package metadata already exists in `mog.toml`, `mog.lock`, and
  `package.api.mog`, with legacy `package.toml` still accepted for
  compatibility

Add a Git module by tag, then install before importing it:

```bash
mog add github.com/acme/math@v1.0.0
mog install
```

```rust
const math = @import("github.com/acme/math")
```

## Reference packages in this repo

- [`github.com/moglang/window`](https://github.com/moglang/window)
- `packages/examples/math/`
- `packages/examples/counter/`

Mog and its package ecosystem are still pre-1.0. Commit `mog.lock` for
applications and use `mog install --locked` in CI when reproducibility matters.
