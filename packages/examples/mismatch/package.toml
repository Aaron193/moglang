namespace = "examples"
name = "mismatch"
version = "0.1.0"
license = "MIT"
abi_version = 3
kelvra_runtime = "^0.2.0"
author = "Kelvra runtime"
description = "Intentionally mismatched package metadata for validation tests."
dependencies = []

[native]
build = "cmake"
targets = ["linux-x86_64-gnu", "linux-arm64-gnu", "macos-arm64"]
